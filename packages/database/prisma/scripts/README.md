# `prisma/scripts/`

- **`ops/`** — maintenance / one-off CLIs (`db:prod-init`, PII waves, i18n sync, demo seed, …). See [`ops/README.md`](ops/README.md).
- **Root of `scripts/`** — bank import/codegen, NAS template seed: `import-banks-md.ts`, `generate-banks-branches-seed.ts`, `seed-nas-accounts.ts`.
