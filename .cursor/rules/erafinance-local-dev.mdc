---
description: ERA Finance — локальная инфраструктура, Windows, Next/Nest, .env
alwaysApply: true
---

# Локальная разработка ERA Finance

## Один источник правды: корневой `.env`

- Скопируй **`.env.example` → `.env`** в **корне** монорепо.
- API читает **`apps/api/.env`** (если есть), затем **корневой `.env`** (`ConfigModule` + пути из `load-env-paths.ts`).
- Команды **`npm run dev:api`**, **`dev:web`**, **`db:*`** из корня подмешивают корневой `.env` через **dotenv-cli**.
- Обязательно задай **`JWT_SECRET`**, **`JWT_REFRESH_SECRET`**, **`DATABASE_URL`**, **`REDIS_URL`**.

## Docker (Postgres + Redis)

1. При необходимости создайте каталоги на **`D:\DockerData\erafinance_erp\`** (тома Docker, кэш npm и т.д.) — вручную или любым удобным способом.
2. `npm run docker:up`.
3. **Первый раз** после пустого тома Postgres: из корня **`npm run db:bootstrap-local`** (`db:migrate` + **`db:seed`** — в т.ч. платформенные super-admin из TS — + **`db:sync-i18n`** из `resources.ts`). Дальше при новых миграциях достаточно **`npm run db:migrate`**; при смене строк в веб-ресурсах — **`npm run db:sync-i18n`** (или prune по правилам деплоя).
4. При необходимости: **`npm run db:migrate`** из корня — это **`prisma migrate deploy`** (подтянуть уже смерженные миграции, **без** вопросов в терминале). Чтобы **создать** новую миграцию из `schema.prisma`: **`npm run db:migrate:dev`** (интерактивно, как раньше `migrate dev`).
5. **TiVi Media / TiVi Sport** (касса, банк, сотрудники, «живой» сценарий) — это **`npm run seed:local -w @erafinance/api`**, не `db:seed:demo`. Каталоги + TiVi подряд: **`npm run db:seed:showcase`**.

### Prisma: «Drift detected» и культура миграций (без reset по умолчанию)

**Drift detected** — `prisma migrate dev` сравнивает **shadow-БД** (все файлы из `prisma/migrations` прогнаны с нуля) с **вашей** БД. Расхождение значит: часть DDL уже есть в PostgreSQL, но **не описана** в истории миграций (типично после `db push`, ручного SQL или **`npm run db:prod-init`**, который дублирует DDL вне migrate).

Это **не** ошибка прав пользователя БД. Код **130** у npm часто бывает при **прерывании** процесса (Ctrl+C) или когда Prisma предлагает reset и оболочка рвёт пайплайн — не обязательно «вы нажали отмену».

**Прод-подобный порядок (сохранить данные):**

1. Подтянуть репозиторий с актуальными миграциями.
2. Из корня выполнить **`npm run db:migrate`** или явно **`npm run db:migrate:deploy`** — одно и то же (`prisma migrate deploy`): применяются только непройденные миграции, **без** shadow-проверки drift.
3. Если нужно убедиться, что схема совпадает с историей и **сгенерировать** новую миграцию: **`npm run db:migrate:dev`** (shadow + при необходимости имя миграции в промпте или флаг `--name` вручную в workspace).

**Reset (`db:migrate:reset`)** — только осознанно для одноразового локального стенда, не как обычный способ «починить» проект.

### Prisma: идемпотентность SQL в `migration.sql`

**Зачем:** `prisma migrate deploy` может повторно прогнать миграцию после сбоя (**P3018**), на БД уже могли появиться объекты из **`db push`**, ручного SQL или **частично применённого** файла миграции. Голый `CREATE TYPE` / `CREATE TABLE` без защиты даёт **42710** (тип уже есть) или рассинхрон «таблица старая, индекс по новой колонке падает».

**Правила при написании/правке миграций в этом репозитории:**

1. **Enum-типы PostgreSQL** — не полагаться на «чистую» БД: оборачивать в `DO $…$ BEGIN … IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = '…') THEN CREATE TYPE … END IF; END $…$;` (или эквивалент), потому что у `CREATE TYPE … AS ENUM` нет переносимого `IF NOT EXISTS` на всех версиях.
2. **`CREATE TABLE`** — по возможности `IF NOT EXISTS`; если из‑за drift возможна **устаревшая таблица без нужных колонок**, явно описать в миграции восстановление: `ALTER TABLE … ADD COLUMN IF NOT EXISTS` и/или условный `DROP TABLE` только при детектируемой «битой» схеме (с узким критерием), либо отдельная follow-up миграция — без широкого `DROP` на прод без причины.
3. **Индексы** — `CREATE INDEX IF NOT EXISTS` / уникальные индексы с тем же именем, что в `@@unique` / `@@index` в Prisma.
4. **Внешние ключи и прочие ограничения** — `ALTER TABLE … ADD CONSTRAINT` внутри `DO` с `EXCEPTION WHEN duplicate_object THEN NULL` (или проверка `pg_constraint`), чтобы повторный прогон не падал.
5. **Уже задеплоенная миграция** в проде **не переписывать** ради идемпотентности — только новая миграция сверху; идемпотентность вносить в **новые** файлы и при необходимости в правки **до** первого прод-деплоя данного файла.
6. После **P3018** Prisma блокирует очередь: **`prisma migrate resolve`** (`--rolled-back` или `--applied` по ситуации) из **`packages/database`** с **`DATABASE_URL`** (корневой `.env` через **`npx dotenv-cli -e .env --`**, как в других скриптах), затем снова **`npm run db:migrate`**.

## Сборка и деплой

- Перед **`npm run build`** в корне автоматически выполняется **`npm run i18n:audit`**: пустые или отсутствующие строки перевода **RU/AZ** в **`apps/web/lib/i18n/resources.ts`** блокируют сборку (и CI, если билд идёт через тот же скрипт).
- Если меняли **`resources.ts`**, в том же PR выполните **`npm run i18n:catalog`** и закоммитьте обновлённый **`apps/api/src/admin/i18n-default-catalog-data.json`** (иначе API и Super-Admin увидят устаревшие дефолты относительно веб-бандла). Произвольные копии каталога в корне репозитория (например `*_i18n-default-catalog*.new.json`) в пайплайн не входят — не использовать как источник правды.
- **Прод после миграций:** из корня **`npm run db:deploy`** — `prisma migrate deploy` + синхронизация **`translation_overrides`** из `resources.ts` с удалением устаревших ключей **ru/az** и bump **`i18n.cacheVersion`**. Локально для мягкой подгрузки без prune: **`npm run db:sync-i18n`**.

### Новые ключи перевода: три разных места (не путать)

Один и тот же текст в коде проходит **три независимых канала**. Команда **`npm run i18n:catalog`** трогает только **один** из них.

| Где живёт | Зачем | Что сделать при новых ключах |
|-----------|--------|--------------------------------|
| **`apps/web/lib/i18n/resources.ts`** | Единственный источник для **Next.js** (`t(...)`, бандл). Без правок здесь в UI не появится строка. | Добавить **и** `ru`, **и** `az` (сборка гоняет **`npm run i18n:audit`**). Динамические ключи (`counterparties.legalForm_LLC` и т.д.) скрипт аудита по `t("...")` не ищет — их всё равно нужно положить в дерево `resources` с тем же путём, что в `t(counterpartyLegalFormI18nKey(...))`. |
| **`apps/api/src/admin/i18n-default-catalog-data.json`** | Снимок дефолтов для **Super-Admin → переводы** и прочих вызовов API с дефолтами из кода. **Не** влияет на сам по себе Postgres и **не** подменяет бандл веба. | **`npm run i18n:catalog`** из корня, затем **закоммитить** обновлённый JSON. Если команда не запускалась или артефакт не задеплоен, в админке «дефолт» по ключу будет пустым/старым, хотя в `resources.ts` всё есть. |
| **Таблица `translation_overrides` (Postgres)** | Оверрайды поверх бандла; отдаёт **`GET /api/public/translations?locale=`** для клиента. | **`npm run db:sync-i18n`** (или **`db:sync-i18n:prune`** / **`npm run db:deploy`** — подтягивает строки из `resources.ts` в БД и поднимает **`i18n.cacheVersion`**). Руками в БД: **`locale`** только в нижнем регистре (`ru` / `az`), ключ — как во flat из ресурсов (`counterparties.legalForm_LLC`, **не** `counterparties.legalForm.LLC`). Опасные родители-строки и «сырой» placeholder см. в **`apps/web/lib/i18n/apply-db-overrides.ts`**; сухой прогон: **`npm run db:audit-i18n-overrides -w @erafinance/database`**. |

**Практический чеклист в одном PR:** правки в **`resources.ts`** → **`npm run i18n:catalog`** + коммит **`i18n-default-catalog-data.json`** → **`npm run db:sync-i18n`** (или deploy-скрипт) → локально перезапуск **`dev:web`** / **`dev:api`** или жёсткое обновление страницы, на проде — новый образ/билд с актуальным JSON и применённой синхронизацией БД.

## Запуск приложений

**Чтобы открывался http://localhost:3000/** — должен быть запущен фронт: **`npm run dev:web`** или **`START-WEB.bat`**.

**Логин / регистрация** ходят на API (**по умолчанию http://127.0.0.1:4000**). Если запущен только фронт, в браузере будет «Failed to fetch» — поднимите API: **`npm run dev:api`** или сразу оба: **`npm run dev`** / **`START-ERP.bat`**.

Чтобы не ловить **EADDRINUSE** на **3000** / **4000**, перед полным стартом используйте **`START-ERP.bat`** (освобождает порты через PowerShell) или вручную: **`npm run stop:next`** / **`npm run stop:api`** (`kill-port`).

**`npm run dev:web`** подмешивает корневой **`.env`** через **`dotenv-cli`**.

Next слушает **`0.0.0.0:3000`** — доступны и `localhost`, и `127.0.0.1`, и LAN.

| Команда | Назначение |
|---------|------------|
| **`npm run dev`** | API + Web: Web ждёт порт **4000** (`wait-on`), при выходе одного процесса падает второй (`-k`). |
| **`npm run dev:api`** | Только Nest (порт **4000**). |
| **`npm run db:seed:showcase`** | После миграций: Prisma `db:seed:demo` + **TiVi** наполнение (`seed:local` в API) — локальный просмотр ERP с данными. |
| **`npm run dev:web`** | Только Next (**3000**). |
| **`npm run dev:web:clean`** | Очистка **`.next`**, затем Next (сначала останови Next, если EPERM). |
| **`npm run restart:web`** | Только Next: `stop:next` + `dev:web:clean`. |
| **`npm run restart:api`** | Только API: `stop:api` + `dev:api`. |

Если запущен **`npm run dev`**, останавливай **одним Ctrl+C** в этом терминале. Не полагайся на **`stop:api`** / **`stop:next`** вместо этого — `concurrently` может остаться в несогласованном состоянии.

## Порядок `.env` в API

Файлы подключаются в порядке: **корень монорепо** → **`apps/api/.env`**. Последний перекрывает предыдущий.

## Windows: освобождение портов

- **`npm run stop:next`** — освободить порт **3000** (`npx kill-port 3000`).
- **`npm run stop:api`** — освободить порт **4000** (`npx kill-port 4000`).

## Next.js и папка `.next`

- Не удалять **`.next`**, пока запущен **`next dev`**.
- Не гонять **`npm run build`** по web параллельно с **`dev:web`** (EPERM / битый кэш).
- При ошибках `routes-manifest` / `962.js`: **`npm run stop:next`**, затем **`npm run dev:web:clean`**.

## Антивирус

- Исключить из сканирования в реальном времени: папку проекта или минимум **`apps/web/.next`**.
